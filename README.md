# mangowm-dotfiles
The X11 folder are configs ONLY for X11

The `picom.conf` file is a file I found on the internet and it needs to be modified to run on certain setups

`alacritty.toml`'s collor pallate is catpuccin


# mango #

My mango config uses `SUPER` as the main key, `rofi -show drun` as the application launcher(SUPER+space), `foot` as the terminal emulator (SUPER+Return)
